version:  '3'
services:
  juice-shop:
    image: bkimminich/juice-shop
    ports:
        - '3000:3000'
  appsec:
    ports:
      - '80:80'
    ipc: host
    image: checkpoint/cloudguard-appsec-standalone
    volumes:
      - ./reverse-proxy/default:/etc/nginx/conf.d/default.conf
      - ./cloudguard-appsec-standalone/data:/etc/cp/data
      - ./cloudguard-appsec-standalone/conf:/etc/cp/conf
      - ./cloudguard-appsec-standalone/logs:/var/log/nano_agent   
    command: ["/cloudguard-appsec-standalone", "--token", "$TOKEN"]
